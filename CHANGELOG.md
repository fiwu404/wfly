# 变更日志

项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，发布标签使用 `v` 前缀。

## [0.0.9] - 2026-08-16

### 修复

- 兼容 Clash/Mihomo 订阅中 VLESS 节点常见的单行 YAML 对象与 `reality-opts` 内嵌对象写法，仍只读取白名单节点字段。

## [0.0.8] - 2026-08-16

### 新增

- 支持受限转换 Clash/Mihomo YAML 的根级 `proxies` 中 VLESS 节点；仅导入节点数据，不执行规则、代理组、锚点、别名或其它 YAML 构造。

### 修复

- 手动更新订阅失败时改为在节点组状态、状态栏和内存日志中显示安全摘要，不再以模态错误框阻断界面。

## [0.0.7] - 2026-08-15

### 变更

- 将可调宽度的导航栏移到窗口左侧，并在右缘保留细分隔线。
- 精简设置页：移除原生配置文件导入行，以及内核下载卡片中的默认内核和下载目录状态文字。

## [0.0.6] - 2026-08-15

### 修复

- 修复设置页卡片的自动尺寸循环：状态行、内核列表和下载按钮现在使用独立的纵向布局行，不会在首次布局或缩放时重叠。

## [0.0.5] - 2026-08-15

### 修复

- 修复节点、节点组、连接、日志和测试页的标题、筛选器、操作按钮与表格重叠问题。
- 统一这些页面为显式纵向布局；规则表格会随编辑区高度填充。

## [0.0.4] - 2026-08-15

### 变更

- 右侧导航改为可拖动比例的细分隔栏，移除深色底色和右下角数据来源。
- 统一页面卡片、列表、按钮为轻量毛玻璃视觉，并重新排版设置页以适应不同窗口宽度。
- 首页移除独立运行控制；三档开关拨到系统代理或 TUN 即启动所选节点，回到中间档即停止内核。
- 实时流量采样由 1 秒缩短至 250 毫秒，并保留约两分钟的图表窗口。
- 移除各页静态说明文字，保留必要的空状态、错误和输入提示。

### 修复

- 内核异常退出、快速模式切换和窗口关闭时同步开关状态并独立恢复系统代理。
- 修复可拖动右栏在 WinForms 构造阶段的尺寸初始化问题。

## [0.0.3] - 2026-08-15

### 新增

- 重构为右侧八页面导航：首页、节点、节点组、连接、规则、日志、测试、设置。
- 运行数据迁移到工作区/发布目录同级 `data/`；首次启动可安全迁移旧 `%LOCALAPPDATA%\WFly` 数据。
- 节点组、节点、规则集、规则条目及应用设置的耐久化存储。
- 节点组订阅刷新：HTTPS 下载、解析、保留节点状态并原子替换该组节点。
- 图形化规则编辑器、JSON 配置文件视图及 `data/rules/` 规则副本。
- 首页三档图形开关、用户触发的出口检测、Google 实际 HTTP 延迟和四曲线流量图。
- 内存连接/进程日志、手动导出日志、网站访问延迟测试。
- Mihomo 官方 Windows x64 内核发现、校验下载与原生配置运行入口。
- 安全的 Windows 系统代理租约恢复和 sing-box TUN 配置生成。

### 变更

- 节点必须归属于节点组；移除“全部”默认分类。
- 订阅 URL 按用户选择保存在本地数据目录以支持更新，但在界面和日志中脱敏。
- 日志仅在用户显式导出时写入磁盘。

### 安全

- 系统代理仅允许设置为本机 `127.0.0.1:<端口>`，恢复前检查所有权，避免覆盖外部变更。
- 启动时会检查上次异常退出遗留的系统代理租约；若仍由 WFly 持有则安全恢复。
- 进入或离开 TUN 时会停止内核，要求重新启动后应用配置，避免遗留 TUN 继续截流。
- TUN 仅在管理员运行的 sing-box 配置中生成，不会自动提权；Mihomo/Xray 原生配置不会被自动写入未知的系统代理端口。

## [0.0.2] - 2026-08-15

### 新增

- HTTPS 订阅链接导入、sing-box 本地配置生成，以及 sing-box/Xray-core 安全内核管理基础。
